public Boolean isPassed(Boolean passedExam, Boolean passedAssignments, Boolean passedProject) {
    //code here
    Integer passingVal = (passedExam ? 1 : 0) + (passedAssignments ? 1 : 0) + (passedProject ? 1 : 0);
    return passingVal >= 2;

}