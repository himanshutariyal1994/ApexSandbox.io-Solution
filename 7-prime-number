public Boolean isPrime(Integer num) {
    if(num == 2 || num == 3) {
        return true;
    }

    //code here
    for(Integer i = 2;i<=Math.floor(Math.sqrt(num));i++) {
        if(Math.mod(num,i) == 0) {
            return true;
        }
    }
    return false;
}