public String formatName(String firstName, String lastName) {
  //code here
  return ((String.isNotBlank(lastName) ? (lastName + ', ') : '') +
    (String.isNotBlank(firstName) ? firstName : ''))
    .trim()
    .removeEnd(',');
}
