public List<String> orgNames(List<String> adjectives, List<String> animals) {
  List<String> orgNamesList = new List<String>();

  for (String adj : adjectives) {
    for (String animal : animals) {
      orgNamesList.add(adj + '-' + animal);
    }
  }
  return orgNamesList;
}
